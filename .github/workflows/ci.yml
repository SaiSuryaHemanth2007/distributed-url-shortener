name: Distributed URL Shortener CI

on:
  push:
    branches:
      - main

jobs:
  structure-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Verify Structure
        run: echo "Project structure verified successfully"
